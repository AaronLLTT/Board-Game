/// @description Draw Power Description

draw_self();

if (image_index == 0) {
	draw_text(x, bbox_bottom + 32, "You can draw two cards and add their values together. You may do this up to 6 times during the game.");
}